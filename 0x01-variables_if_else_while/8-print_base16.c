#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Succes)
 */
int main(void)
{
	int f;

	for (f = 48; f < 58; f++)
	{
		putchar(f);
	}
	for (f = 97; f < 103; f++)
	{
		putchar(f);
	}	
        putchar('\n');
	return (0);
}		
